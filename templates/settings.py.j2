"""
Django settings for {{ waj_project_title }} project.

Generated by 'django-admin startproject' using Django 2.2.3.

For more information on this file, see
https://docs.djangoproject.com/en/2.2/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/2.2/ref/settings/
"""

import os
from django.contrib.messages import constants as messages
from django.utils.translation import gettext_lazy
import sentry_sdk
from sentry_sdk.integrations.django import DjangoIntegration
from .base_settings import *

# Build paths inside the project like this: os.path.join(BASE_DIR, ...)
BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))

# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/2.2/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = '{{ waj_secretkey }}'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = {{ waj_settings_debug }}

ALLOWED_HOSTS = [{{ waj_domain_names | map('to_json') | join(', ') }}]
CSRF_TRUSTED_ORIGINS = [{{ waj_csrf_trusted_origins | map('to_json') | join(', ') }}]
CSRF_ALLOWED_ORIGINS = [{{ waj_csrf_trusted_origins | map('to_json') | join(', ') }}]
CORS_ORIGINS_WHITELIST = [{{ waj_csrf_trusted_origins | map('to_json') | join(', ') }}]

if DEBUG:
    INSTALLED_APPS += [
        'debug_toolbar',
        ]

    MIDDLEWARE += [
        'debug_toolbar.middleware.DebugToolbarMiddleware',
    ]

WSGI_APPLICATION = '{{ waj_project_title }}.wsgi.application'

# Database
# https://docs.djangoproject.com/en/2.2/ref/settings/#databases


DATABASES = {
{% if waj_db_type == "mariadb" %}
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': '{{ waj_mysql_db_name }}',
        'USER': '{{ waj_mysql_user_name }}',
        'PASSWORD': '{{ waj_mysql_user_password }}',
        'HOST': '{{ waj_mysql_user_host }}',
        'PORT': '',
        'OPTIONS': {
            'charset': 'utf8mb4',
            'init_command': "SET NAMES utf8mb4 COLLATE utf8mb4_unicode_ci",
        },
    }
{% else %}
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': os.path.join(BASE_DIR, 'db.sqlite3'),
    }
{% endif %}
}

# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/2.2/howto/static-files/

STATIC_URL = '/static/'
STATIC_ROOT = os.path.join(BASE_DIR, 'staticfiles')

STATICFILES_DIRS = [
    os.path.join(BASE_DIR, 'static')
]

LOCALE_PATHS = [
    os.path.join(BASE_DIR, 'translations', 'locale'),
]

MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')

LOGIN_URL = '/account/login/'
LOGIN_REDIRECT_URL = '/admin_portal/'
LOGOUT_REDIRECT_URL = '/'

DEFAULT_FROM_EMAIL = 'noreply@{{ waj_domain_names | first }}'
ACCOUNT_MODERATOR_EMAILS = []
ACCOUNT_EMAIL_FAIL_SILENTLY = False

# Cache backend configuration
# Local default: FileBasedCache (persists across webserver restarts).
# Production Redis: set these environment variables:
# - DJANGO_CACHE_BACKEND=django_redis.cache.RedisCache
# - DJANGO_CACHE_LOCATION=redis://127.0.0.1:6379/1 (adjust host/db)
# Also install django-redis in the production environment.
DJANGO_CACHE_BACKEND = os.getenv('DJANGO_CACHE_BACKEND', 'django.core.cache.backends.filebased.FileBasedCache')
DJANGO_CACHE_LOCATION = os.getenv('DJANGO_CACHE_LOCATION', os.path.join(BASE_DIR, '.django_cache'))

if DJANGO_CACHE_BACKEND == 'django.core.cache.backends.filebased.FileBasedCache':
    os.makedirs(DJANGO_CACHE_LOCATION, exist_ok=True)

CACHES = {
    'default': {
        'BACKEND': DJANGO_CACHE_BACKEND,
        'LOCATION': DJANGO_CACHE_LOCATION,
    }
}

if DJANGO_CACHE_BACKEND == 'django_redis.cache.RedisCache':
    CACHES['default']['OPTIONS'] = {
        'CLIENT_CLASS': 'django_redis.client.DefaultClient',
    }

DISABLE_CACHE_FOR_DEBUG = os.getenv('DISABLE_CACHE_FOR_DEBUG', 'False').lower() == 'true'
if DISABLE_CACHE_FOR_DEBUG:
    CACHES['default'] = {
        'BACKEND': 'django.core.cache.backends.dummy.DummyCache',
    }

THUMBNAIL_ENGINE = 'sorl.thumbnail.engines.pil_engine.Engine'
THUMBNAIL_DEBUG = True

# Geo-location based routing
# Redirect users from Netherlands to Dutch domain, others to English domain
GEO_REDIRECT_ENABLED = os.getenv('GEO_REDIRECT_ENABLED', 'False').lower() == 'true'
GEO_REDIRECT_NL_DOMAIN = '{{ waj_geo_redirect_nl_domain }}'  # Dutch domain
GEO_REDIRECT_EN_DOMAIN = '{{ waj_geo_redirect_en_domain }}'  # English domain (fallback)

# Monitoring application
if not DEBUG:
    sentry_sdk.init(
        dsn="https://3caa949e65db4d419d486668fe5180a4@sentry.io/1527507",
        integrations=[DjangoIntegration()]
    )

# Debug toolbar settings
INTERNAL_IPS = [
    '127.0.0.1',
    {% if not waj_production %}
    # Allow Debug toolbar for the developers
    {{ waj_developer_ips | map('to_json') | join(', ') }}
    {% endif %}
]

SECURE_BROWSER_XSS_FILTER = True
SECURE_CONTENT_TYPE_NOSNIFF = True
#SECURE_SSL_REDIRECT = True
SESSION_COOKIE_SECURE = not DEBUG
CSRF_COOKIE_SECURE = not DEBUG
SECURE_HSTS_SECONDS = 86400  # 1 day
SECURE_HSTS_INCLUDE_SUBDOMAINS = True
SECURE_HSTS_PRELOAD = True

DEFAULT_AUTO_FIELD = 'django.db.models.AutoField'

DISABLED_BIBLE_TRANSLATIONS = []

BIBLE_API_KEY = '{{ waj_bible_api_key }}'  # The API key to scripture.api.bible

DEFAULT_BIBLE_ANY_LANGUAGE = 'de4e12af7f28f599-02'
DEFAULT_BIBLE_PER_LANGUAGE = {
    'en': 'de4e12af7f28f599-02', # King James Version (Protestant)
    'nl': '87f620660790371b-01', # Herziene Statenvertaling
}

YOUTUBE_API_KEY = '{{ waj_youtube_api_key | default("") }}'  # The API key to YouTube Data API v3

{% for setting_name, setting_value in waj_extra_settings | dictsort %}
{{ setting_name }} = '{{ setting_value }}'
{% endfor %}
